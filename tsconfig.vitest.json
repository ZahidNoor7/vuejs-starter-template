{
  "extends": "./tsconfig.app.json",
  "include": [
    "**/*.*", // Include all files in the project directory and its subdirectories
    "src/**/*.ts", // Include TypeScript files in the src directory and its subdirectories
    "utils/**/*.ts", // Include TypeScript files in the utils directory and its subdirectories
    "vite-env.d.ts"
  ],

  "exclude": [],
  "compilerOptions": {
    "composite": true,
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.vitest.tsbuildinfo",

    "lib": [],
    "types": ["node", "jsdom"]
  }
}
